version: "3.9"
services:
  rosys:
    image: "zauberzeug/rosys:${TAG-latest}"
    volumes:
      - ./:/rosys
      - ./:/app
      - ./../nicegui/nicegui:/usr/local/lib/python3.9/site-packages/nicegui
      - ~/.rosys:/root/.rosys
    privileged: true
    environment:
      - TZ=Europe/Amsterdam
    build:
      context: ./
      args:
        INSTALL_DEV: "true"
    cap_add:
      - SYS_NICE # allow container to set process priorities
      - SYS_PTRACE # allow profiling with py-spy
    hostname: docker
    logging:
      driver: "json-file"
      options:
        max-size: "200m"
        max-file: "100"
    command: /rosys/start.sh debug
